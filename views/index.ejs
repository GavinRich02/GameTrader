<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameTrader</title>
    <link rel="stylesheet" href="/index.css" type="text/css">
    <script src="/main.js" defer></script>
</head>
<body>
    <div id="header">
        <h1 id="head" style="display: inline-block">GameTrader</h1>
        <form action="/items/new/<%=user.id%>" method="GET" style="display: inline-block;"><button>+</button></form>
    </div>
    <% items.forEach(item=> { %>
        <% if(item.name!=" ") { %>
            <form action="/items/<%=item.id%>-<%=user.id%>?_method=PATCH" method="POST" id="patch-<%=item.id%>">
                <div class="item" onclick="document.getElementById('patch-<%=item.id%>').submit()">
                    <h1><%= item.name %></h1>
                    <h1>Views: <%= item.views %></h1>
                    <h1>$<%= item.cost%></h1>
                </div>
            </form>
        <% } %>
    <% }) %>
</body>
</html>